{
  "name": "svg-foos",
  "version": "1.0.0",
  "description": "The purpose of this is to demonstrate various ways that SVGs can be implemented.",
  "main": "index.js",
  "scripts": {
    "sprite": "svg-sprite --symbol --symbol-dest=svg --symbol-sprite=sprite.svg --dest=dist/assets/ src/assets/svg/*.svg",
    "browser": "open-cli 'file:///Users/greg.craft/workspace/asynchrony/ux-huddles/svg/dist/index.html'",
    "build": "rm -rf dist && npm run sprite && cp src/index.html dist && npm run browser"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "open-cli": "^1.0.5",
    "svg-sprite": "^1.3.6"
  }
}
